*.o
*.a
*~
*.so
*.bak
*.pyc
*-orig
*-sav
foo*
goo*
conf.h
config.status
config.log
config.cache
debug*.html
debug*.py
TODO
BUGS
tpos
js9helper
js9helper.pc
.DS_Store
a.out.dSYM
/Makefile
/src/Makefile
/astroem/jsfitsio/Makefile
/astroem/wrappers/Makefile
/data
/tests2
/notes
/old
/tmp
/talks

# Node dependencies — installed via `npm install`, not committed.
# (package.json still lists bundledDependencies; npm bundles those at publish
#  time from node_modules, so untracking here does not affect `npm publish`.)
node_modules/

# Playwright test artifacts
/test-results/
/playwright-report/

# Generated build artifacts — produced by `npm run build` (scripts/build.mjs),
# not committed. A clone gets them via the `prepare` hook on `npm install`;
# `npm publish` ships them (see .npmignore). Sources (js9.js, js9.css, css/,
# js/, plugins/, astroem*) are NOT here — only build outputs.
/js9support.js
/js9support.min.js
/js9support.css
/js9support.txt
/js9plugins.js
/js9plugins.min.js
/js9.min.js
/js9-allinone.js
/js9-allinone.css
